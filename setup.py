from setuptools import setup, find_packages

setup(
    name='image-detector-library',
    version='0.1',
    description='python library to find images in images',
    author='jules1468',
    packages=find_packages(),
)